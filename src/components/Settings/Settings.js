import React from "react";
import Settings_classes from './Settings.module.css';

const Settings = (props) => {
    return (
        <div>
            Settings
        </div>
    )
}

export default Settings;